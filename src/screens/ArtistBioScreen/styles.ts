import { StyleSheet } from 'react-native';

export const cs = StyleSheet.create({
    container: {
        marginHorizontal: 16,
    },
    nameArtistText: {
        fontSize: 20,
        fontWeight: '500',
        textAlign: 'center',
        marginTop: 10,
    },
    bioText: {
        fontWeight: '600',
        fontSize: 20,
        color: '#111',
        marginVertical: 10,
    },
});

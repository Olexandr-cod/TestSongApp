import { StyleSheet } from 'react-native';

export const cs = StyleSheet.create({
    container: {
        marginBottom: 23,
        alignItems: 'center',
    },
    impStyle: {
        height: 140,
        width: 140,
        marginBottom: 5,
    },
});
